# Restaurant Finder

A simple project for locating restaurants using the Google Maps Places API.

## Running Tests

This project uses Jest with the jsdom environment for testing. After installing dependencies run:

```bash
npm test
```

This will execute all tests located in the `tests` folder.
